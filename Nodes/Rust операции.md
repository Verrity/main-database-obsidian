---




---
---
* [[Rust|Назад]]
---
### `iter().map()`
Применяет функцию к каждому элементу итератора, возвращая новый итератор с преобразованными значениями.
Создаёт ленивый итератор
```rust unfold
.map(|element| { /* fund(element) */ }
```